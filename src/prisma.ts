import { PrismaClient } from "@prisma/client"; // Import PrismaClient from @prisma/client

const prisma = new PrismaClient(); // Instantiate PrismaClient

export default prisma; // Export the instantiated PrismaClient
